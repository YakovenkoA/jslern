// underfined - неопределенный тип

let userName; // определяем переменную
// получаем тип данных
console.log(typeof userName);
// получаем значение переменной
console.log(userName);


///////////////////////////////////

// Null           содержит только одно значение

let useName = null;
console.log(useName);

//////////////////////////////////////////////

// Boolean      true       false

let willYouMe = true;
if (willYouMe) {
    console.log(':)');
} else {
    console.log(':(');
}